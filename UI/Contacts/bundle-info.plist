{
  requires = {
    bundleManagerVersion = 1;
    classes = (
      { name = NSObject;    }
    );
  };

  provides = {
    classes = (
      { name = UIxContactEditor; }
    );
    WOComponents = (
      { name = UIxContactEditor; }
    );
  };
}
